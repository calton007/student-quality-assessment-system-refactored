# TODO: 纯 C++ 工程项目转型

## 1. 工程运行基础

- [x] 新增 `AppConfig`，解析默认数据目录
- [x] 支持命令行参数 `--data-dir <path>`
- [x] 默认运行数据目录改为 `%LOCALAPPDATA%\StudentQualityAssessment`
- [x] 保留 `User.txt` 作为默认账号配置文件
- [x] README 更新为工程项目说明，不再写“课程设计项目”

## 2. 日志与错误处理

- [x] 新增 `AppLogger`
- [x] 记录程序启动、登录失败、生成综测、保存失败、异常退出
- [x] 致命错误直接退出
- [x] 普通业务错误统一由 UI 显示

## 3. 控制台 TUI

- [x] 新增 `ConsoleScreen`
- [x] 新增 `ConsoleMenu`
- [x] 新增 `ConsoleTable`
- [x] 新增 `ConsoleTheme`
- [x] 统一标题栏、当前位置、当前用户、菜单、表格、提示信息
- [x] 所有表格支持中文宽度对齐
- [x] 空数据时显示明确提示

## 4. 输入体验

- [x] 统一菜单选择输入
- [x] 统一文本输入
- [x] 统一分数输入
- [x] 统一确认操作
- [x] 支持返回和取消
- [x] 非法输入不崩溃，提示后重新输入

## 5. 三类用户菜单改造

- [x] 改造学生菜单
- [x] 改造辅导员菜单
- [x] 改造测评小组菜单
- [x] 每个页面显示当前位置和当前登录用户
- [x] 每次录入前显示正在操作的学生、项目和数据类型

## 6. 数据安全与恢复

- [x] 运行数据继续使用 DPAPI 加密
- [x] 保存前生成 `.bak` 备份
- [x] 写入失败时保留旧文件
- [x] 数据文件损坏时直接报错退出
- [x] 不自动修复损坏数据
- [x] `User.txt` 只保存账号配置，测评流程状态迁移到 `AssessmentStatus.txt`
- [x] 总分生成后锁定评分依据，撤销后才能修改

## 7. 测试

- [x] 测试 `AppConfig`
- [x] 测试菜单输入
- [x] 测试分数输入
- [x] 测试中文表格输出
- [x] 测试 DPAPI 保存后不是明文
- [x] 测试备份文件生成
- [x] 测试保存失败不破坏旧数据
- [x] 回归测试默认账号完整流程

## 8. 收尾

- [x] 清理根目录编译产物
- [x] 检查 `.gitignore`
- [ ] 编译主程序 Debug/Release x64
- [ ] 运行测试项目
- [x] Review 查 bug
- [x] 第一性原理检查：是否更简单、更稳、更少状态
